# Copy to .env and adjust if you are not using the bundled docker-compose Postgres.
DATABASE_URL="postgresql://pulse:pulse@localhost:5433/pulse?schema=public"

# Base URL the scripts/ helpers point at.
PULSE_BASE_URL="http://localhost:3000"
